# libft

## Description
"Crwate your very first own library" is the task of the first project at Codam.
Libft consists of a static library with reporduced standard C functions which ompile with a Makefile.
The library will be used for all further projects.

## Usage

make without bonus functions

make bonus to compile with bonuses
